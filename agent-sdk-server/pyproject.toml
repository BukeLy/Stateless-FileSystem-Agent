[project]
name = "agent-sdk-server"
version = "0.1.0"
description = "Claude Agent SDK Lambda container"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.34.0",
    "claude-agent-sdk>=0.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
