[project]
name = "agent-sdk-client"
version = "0.1.0"
description = "Telegram webhook handler for Claude Agent"
requires-python = ">=3.12"
dependencies = [
    "python-telegram-bot>=21.0",
    "httpx>=0.27.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
